Review and lightly improve this tutorial chapter for quality.
Language: {{ language }}
Requirements:
- Keep the same overall structure and heading order.
- Ensure at least {{ need }} mermaid diagrams (currently {{ have }}).
- Prefer real file paths already mentioned; do not invent new modules.
- Fix broken mermaid if needed (short labels, sequenceDiagram participant aliases).
- Keep length similar; do not rewrite from scratch unless necessary.
- Output ONLY the full corrected markdown chapter.

Chapter:
{{ chapter_md }}
