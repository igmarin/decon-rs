[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "python-lib"
version = "0.1.0"
description = "Tiny test fixture library"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
mylib-cli = "mylib.cli:main"
